import React, {
  FC,
  useEffect,
  useState,
  useReducer,
  useCallback,
  useRef,
  useMemo,
} from 'react';
import ReactDOM from 'react-dom';
//@ts-ignore
import { v1 as uuidv1 } from 'uuid';
import { WindowsOutlined } from '@ant-design/icons';
import { Form, Modal, Drawer } from 'antd';
import styles from './index.less';

const Page: FC<any> = props => {
  return <div></div>;
};

export default Page;
